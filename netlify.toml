[build]
  command = "npm run build:client"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_BASE_URL = "https://srpizzza.onrender.com"
# Set this in Netlify UI (or here) to point the frontend at your backend.

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
